# RAG 智能助手前端重构需求文档（登录/鉴权版）

## 1. 项目目标
- 将现有前端升级为 ChatGPT 风格的 C 端产品体验：极简、沉浸、以对话为核心。
- 增加完整账号体系：手机号注册/登录、JWT 鉴权、设置中心、安全退出。
- 支持沙箱环境运行：前端可在无真实生产依赖时进行完整功能演示与联调。

## 2. 适用范围
- Web 前端（Vue 3 + Element Plus + Tailwind CSS）。
- 用户侧功能：认证、会话、知识库问答、设置页。
- 不包含：运营后台、复杂计费系统、多租户管理后台。

## 3. 产品定位与体验原则
- 风格对标 ChatGPT：轻界面层级、弱边框、强留白、突出输入与消息内容。
- 交互优先级：提问效率 > 阅读体验 > 配置管理。
- 安全优先：所有核心接口默认需要 JWT 鉴权。

## 4. 用户角色与权限
- 游客（未登录）
  - 可访问：登录/注册页。
  - 不可访问：聊天页、历史会话、设置页、知识库操作。
- 登录用户（普通用户）
  - 可访问：聊天、上传文档、历史会话、设置、退出登录。
  - 权限边界：仅可访问本人会话与本人上传文件。

## 5. 核心功能需求

### 5.1 手机号注册与登录
- 注册方式：手机号 + 验证码 + 密码（可选邀请项后续扩展）。
- 登录方式：手机号 + 密码（一期）；验证码快捷登录（二期预留）。
- 表单校验：
  - 手机号：11 位中国大陆手机号格式校验。
  - 密码：8-20 位，至少包含字母与数字。
  - 验证码：6 位数字，时效 5 分钟。
- 失败反馈：统一错误提示，不暴露敏感信息（如“账号或密码错误”）。

### 5.2 JWT 鉴权机制
- 登录成功后返回 `access_token` 与 `refresh_token`。
- 前端请求头统一附加：`Authorization: Bearer <access_token>`。
- 令牌策略：
  - `access_token` 短时有效（建议 30-120 分钟）。
  - `refresh_token` 长时有效（建议 7-30 天）。
- 过期处理：
  - 401 时自动尝试刷新 token。
  - 刷新失败则清理本地凭证并跳转登录页。
- 安全退出：
  - 主动退出时清空本地会话状态与 token。
  - 若后端支持，调用注销接口使 refresh_token 失效。

### 5.3 页面级鉴权与路由守卫
- 路由分组：
  - 公共路由：`/login`、`/register`。
  - 私有路由：`/chat`、`/settings`。
- 守卫逻辑：
  - 无 token 访问私有路由时重定向到 `/login`。
  - 已登录访问 `/login` 时重定向到 `/chat`。

### 5.4 ChatGPT 风格主页面
- 左侧 Sidebar（桌面端 260px，移动端抽屉）：
  - 新建对话按钮。
  - 历史会话列表（标题、更新时间、选中态）。
  - 设置入口按钮（底部）。
  - 用户头像与昵称（底部）。
- 右侧 Main：
  - 顶部极简栏（当前会话标题、模型状态）。
  - 中部消息区（max-width: 800px，滚动区）。
  - 底部固定输入区（支持文本 + 附件上传）。

### 5.5 设置中心（Settings）
- 入口：Sidebar 底部“设置”按钮。
- 模块：
  - 账号信息：头像、昵称、手机号（脱敏展示）。
  - 安全设置：修改密码、退出所有设备（后端支持时）。
  - 偏好设置：主题模式（浅色/深色预留）、回车发送开关、回答渲染偏好。
  - 关于与版本：当前前端版本、环境标识（Sandbox/Prod）。

### 5.6 对话与文档能力（鉴权后）
- 保留并升级现有 RAG 交互：
  - 文档上传并解析状态展示。
  - 智能问答与来源引用展示。
  - 新建会话、切换会话、历史回看。
- 所有 RAG 接口调用均走 JWT 鉴权。

## 6. 沙箱环境需求（Sandbox）
- 目标：开发/测试人员无需真实短信网关和生产用户数据，也可演示完整流程。
- 环境隔离：
  - 独立环境变量：`VITE_APP_ENV=sandbox`。
  - 独立 API Base URL：`VITE_API_BASE_URL`。
- Mock 能力：
  - 提供 mock 登录/注册/token 刷新接口。
  - 提供 mock 会话列表与问答延迟响应。
  - 可配置开关：`VITE_USE_MOCK=true/false`。
- 沙箱账号策略：
  - 预置测试手机号段与固定验证码（仅沙箱）。
  - 页面显式标注“Sandbox”角标，防止误判为生产。

## 7. 视觉与设计规范（ChatGPT 风格）
- 主背景：`#FFFFFF` 或 `#F9FAFB`。
- 主文字：`#111827`；次级文字：`#6B7280`。
- 强调色：`#4F46E5`（仅用于主按钮、激活态、链接焦点）。
- 用户气泡：`#F3F4F6`；AI 内容区默认透明背景。
- 圆角：`12px`-`16px`；阴影：`0 2px 12px rgba(0,0,0,0.04)`。
- 字体：`"PingFang SC", "Inter", "Segoe UI", sans-serif`，行高 `1.6-1.8`。
- 动效：统一 `200ms-300ms ease-in-out`，避免花哨动画。

## 8. 技术方案与前端架构
- 技术栈：
  - Vue 3 + Composition API + `<script setup>`。
  - Element Plus（按需）+ Tailwind CSS + Lucide Icons。
- 状态管理：
  - 认证状态：`auth store`（token、用户信息、登录态）。
  - 业务状态：会话列表、当前会话、上传任务状态。
- 网络层：
  - 封装 `request` 模块，统一注入 token、错误处理、刷新重试。
- 代码组织建议：
  - `views/auth`：登录/注册页。
  - `views/chat`：聊天主页面。
  - `views/settings`：设置页面。
  - `stores/auth`、`stores/chat`。
  - `api/auth`、`api/chat`。

## 9. 接口契约（前后端对齐草案）
- 认证相关：
  - `POST /api/auth/send-code`：发送短信验证码。
  - `POST /api/auth/register`：手机号注册。
  - `POST /api/auth/login`：账号登录。
  - `POST /api/auth/refresh`：刷新 token。
  - `POST /api/auth/logout`：注销（可选）。
- 用户相关：
  - `GET /api/user/profile`：获取用户资料。
  - `PUT /api/user/profile`：更新用户资料。
  - `PUT /api/user/password`：修改密码。
- RAG 相关（需 JWT）：
  - `POST /api/rag/upload`。
  - `GET /api/rag/upload-task/{taskId}`。
  - `POST /api/rag/query`。
  - `GET /api/rag/sessions`。

## 10. 关键业务流程
- 新用户注册：
  - 输入手机号 -> 获取验证码 -> 设置密码 -> 注册成功 -> 自动登录 -> 进入聊天页。
- 登录续期：
  - access_token 过期 -> 自动 refresh -> 重放原请求。
- 登录失效：
  - refresh 失效 -> 跳转登录 -> 保留“登录后继续原会话”的跳转参数。

## 11. 安全与合规要求
- 前端不记录明文密码与验证码日志。
- token 存储策略可配置（优先 HttpOnly Cookie；若当前后端限制则短期使用安全存储并加固）。
- 所有敏感操作需二次校验（如修改密码）。
- 手机号显示脱敏（如 `138****1234`）。

## 12. 非功能性指标
- 首屏可交互时间（登录页）<= 2.5s（常见办公网络）。
- 页面切换平均响应 <= 300ms（不含接口耗时）。
- 关键操作成功率 >= 99%（登录、发送消息、上传文档）。
- 主流浏览器兼容（Chrome、Edge 最新两个大版本）。

## 13. 验收标准（UAT）
- 认证：
  - 可完成手机号注册与登录。
  - token 过期可自动续期，失败可回登录页。
  - 未登录无法进入聊天页与设置页。
- 功能：
  - 设置入口可用，账户信息可查看与更新。
  - 上传文档、发问、查看引用来源流程可闭环。
- 体验：
  - 页面整体风格符合 ChatGPT 极简对话范式。
  - 移动端 Sidebar 抽屉交互正常。
- 沙箱：
  - 在 `sandbox + mock` 条件下可完整演示注册、登录、聊天、设置流程。

## 14. 版本规划建议
- Phase 1（1-2 周）：
  - 登录/注册页、JWT 鉴权、路由守卫、ChatGPT 风格主框架。
- Phase 2（1-2 周）：
  - 设置中心、token 刷新完善、沙箱 mock 全链路。
- Phase 3（1 周）：
  - 体验优化（动画、空状态、错误态）、安全加固与回归测试。

## 15. 风险与依赖
- 依赖后端提供稳定认证接口与 token 刷新机制。
- 短信网关在沙箱环境通常不可用，需 mock 替代。
- 若历史接口未鉴权，需要后端统一改造或网关层补齐鉴权校验。
